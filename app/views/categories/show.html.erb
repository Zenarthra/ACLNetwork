<center><%= render 'shared/page_title', title: "#{@category.name}" %></center>
<% if @category.articles.count != 0 %>
  <h4 align="center">Article's containing: <%= @category.name %></h4>
  <div align="center">
    <%= will_paginate @category_articles %>
  </div>
  
  
  <%= render 'categories/article' %>
  
  
  <div align="center">
    <%= will_paginate @category_articles %>
  </div>
<% else %>
  <h4 align="center">No articles yet!</h4>
<% end %>